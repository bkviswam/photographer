spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql}.${NAMESPACE:photographer-dev}.svc.cluster.local:3306/${DB_NAME:photographer}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USER:app_user}
    password: ${DB_PASSWORD:1q2w3e4r}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8080
management.endpoints.web.exposure.include: health,info